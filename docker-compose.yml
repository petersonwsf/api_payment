version: '3.7'

services:
  postgresql:
    container_name: payment_database
    image: bitnami/postgresql:latest
    ports:
      - 5433:5432
    environment:
      - POSTGRESQL_USERNAME=root
      - POSTGRESQL_PASSWORD=root
      - POSTGRESQL_DATABASE=paymentService
    volumes:
      - postgres_data_payment:/bitnami/postgresql
  postgressqlteste:
    image: bitnami/postgresql:latest
    container_name: bd_teste
    ports: 
      - 7000:5432
    environment:
      - POSTGRESQL_USERNAME=user
      - POSTGRESQL_PASSWORD=user
      - POSTGRESQL_DATABASE=paymentServiceTeste
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - 5672:5672
      - 15672:15672
    environment:
      - RABBITMQ_DEFAULT_USER=admin
      - RABBITMQ_DEFAULT_PASS=admin
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    restart: unless-stopped

volumes:
  postgres_data_payment:
    driver: local
  rabbitmq_data: